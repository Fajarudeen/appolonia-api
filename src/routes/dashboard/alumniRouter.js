import express from 'express';
// import { uploadImageFile } from '../../utils/fileUploader.js';

export const alumniRouter = express.Router();

// alumniRouter.post('/', uploadImageFile('alumni').single('profileImage'), createAlumni);

// alumniRouter.get('/:id', getAlumniById);

// alumniRouter.get('/', getAlumniTable);

// alumniRouter.put('/:id', uploadImageFile('alumni').single('profileImage'), updateAlumni);

// alumniRouter.delete('/:id', deleteAlumni);
